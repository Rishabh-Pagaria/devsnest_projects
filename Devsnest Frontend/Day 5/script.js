// const input = document.querySelector('input');
// const numKeys = document.querySelectorAll('.num-key');
// const opKeys = document.querySelectorAll('.op-key');
// let buffer = [];

// numKeys.forEach(num => {
//     num.addEventListener('click',(e)=>{
//         const numClick = e.target;
//         input.value = input.value != 0 ? input.value + numClick.innerText : numClick.innerText;
//         let inputValue =
//         buffer.push(input.value);
        
//     })
// })
// console.log(buffer);
// opKeys.forEach(opp => opp.addEventListener('click',e =>{
//     const keys = e.target;
//     const action = keys.dataset.action;
//     console.log(action);
    
// }))

// numKeys.forEach(num => {
//     num.addEventListener('click', ()=> input.value = input.value != 0 ? input.value + num.innerText : num.innerText);
// })
// console.log(input.value);
// const compute = ()=>{
    
// }

// opKeys.forEach(opp => opp.addEventListener('click',compute));